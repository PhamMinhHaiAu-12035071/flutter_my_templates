import {Styles} from "ink/build/styles"

const styles = {
	container: {
		display: 'flex',
		flexDirection: 'column',
	} as Styles,
	wrapperTitle: {
		display: 'flex',
		flexDirection: 'row',
		justifyContent: 'center',
	} as Styles,
}

export {
	styles,
}

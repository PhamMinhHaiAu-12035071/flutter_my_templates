import { Styles } from 'ink/build/styles';

const styles = {
  wrapperNotify: {
    marginTop: 2,
    marginBottom: 1,
    height: 1,
    display: 'flex',
    flexDirection: 'row',
  } as Styles,
};

export { styles };

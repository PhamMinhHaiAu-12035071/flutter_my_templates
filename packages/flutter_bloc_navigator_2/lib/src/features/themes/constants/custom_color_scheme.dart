import 'package:flutter/material.dart';

/// Set color common for all themes.
///
extension CustomColorScheme on ColorScheme {
  Color get white => const Color(0xFFFFFFFF);
  Color get black => const Color(0xFF000000);
}

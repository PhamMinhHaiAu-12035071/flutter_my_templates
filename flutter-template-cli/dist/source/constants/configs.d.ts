interface Configs {
    NODE_ENV: string;
    PATH_FOLDER_BIN: string;
    PATH_FOLDER_FLUTTER_TEMPLATE: string;
    LIST_PATH_ALLOWED_COPY: string;
    PATH_FOLDER_TEMPLATE: string;
}
declare const sanitizedConfig: Configs;
export default sanitizedConfig;

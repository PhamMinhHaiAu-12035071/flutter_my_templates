pre-commit:
  parallel: true
  commands:
    commander-lint:
      glob: "*.*"
      run: |
        cd commander
        npm run lint
    commander-format:
      glob: "*.*"
      run: |
        cd commander
        npm run format